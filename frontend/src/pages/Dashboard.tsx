/**
 * Login Page
 *
 * Kullanıcı giriş ekranıdır.
 * Şu an sadece test amaçlı Fake Login içerir.
 *
 * İleride:
 * Backend'e POST /auth/login isteği gönderilecektir.
 */

export default function Login() {
return <h1>Login Page</h1>
}
